---
version: "2.0"

interface_up:
  type: direct

  description: Interface up.

  tasks:
    rais_interface:
      description: Rais the interface up
      action: std.ssh
      input:
        host: 144.217.247.102
        username: ubuntu
        password: password
        cmd: "/opt/mess_it_up/if_up.sh"
